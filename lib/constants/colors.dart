import 'package:flutter/material.dart';

class AppColors {
  static const Color deepPurple = Color(0xFF5E60CE);
  static const Color mintGreen = Color(0xFF80ED99);
  static const Color darkNavy = Color(0xFF1B1B2F);

  // For glassmorphic frosted layers
  static const Color glassWhite = Colors.white; // we’ll add opacity where used
}
